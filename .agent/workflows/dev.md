---
description: 启动开发服务器进行本地开发。
---

使用 `pnpm dev` 启动应用。此命令会并行启动 Next.js 服务器和相关的本地监控脚本。

// turbo
1. 运行开发命令：
   ```powershell
   pnpm dev
   ```

2. 访问 [http://localhost:5401](http://localhost:5401) 查看应用。
